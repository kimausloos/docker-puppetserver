#!/bin/sh

cat <<EOF
This is an S2I puppetserver 5 image responsible for running puppet code
To use it, install S2I: https://github.com/openshift/source-to-image.
Sample invocation:
s2i build git://github.com/cegeka/docker-puppetserver openshift/puppetserver-5-rhel7 puppetservertest
You can then run the resulting image via:
docker run -p 8080:8080 puppetservertest
EOF
