#!/bin/bash -e
#
# S2I run script for the 'puppetserver' image.

exec echo $CAKEY > /etc/puppetlabs/puppet/ssl/ca/ca_key.pem
exec openssl ca -config /certs/openssl_ca.cnf -gencrl -out /etc/puppetlabs/puppet/ssl/ca/ca_crl.pem
exec /usr/local/bin/start-puppet-server
